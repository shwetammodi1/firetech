# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Lockfile intentionally not committed: floating WASM-fallback transitive
# deps (@emnapi/*, rolldown/tailwind wasm bindings) resolve to different
# versions per-platform, breaking `npm ci`. CI/CF fall back to `npm install`.
package-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
